.container
  %h1 Edit Review
  = simple_form_for ([@hotel, @review]) do |f|
    #rating-form
      %label Rating
    = f.input :comment
    = f.button :submit

  :javascript
    $('#rating-form').raty({
      path: '/assets/',
      scoreName: 'review[rating]'
    });